{{- if .Values.serviceAccount.create }}
# 운영의 admin-prod ServiceAccount는 차트 밖에서 만들어져 있어, 차트가 만들려 하면 소유권 충돌로 배포가 실패한다.
# ~/admin_be/helm/admin-prod/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: admin-prod
  namespace: default
{{- end }}
